/**
 * @ngdoc constant
 * @name <%= appname %>.<%= codeName %>
 *
 * @description
 * TODO - Complete this description with full details about this constant.
 */
angular.module('<%= appname %>').constant('<%= codeName) %>', {
    name: 'value'   // Replace this and add additional correct name value properties
});
